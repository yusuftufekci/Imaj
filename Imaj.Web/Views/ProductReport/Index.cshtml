@using Microsoft.AspNetCore.Mvc.Localization
@using Imaj.Web
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<SharedResource> Localizer
@model ProductReportViewModel

@{
    ViewData["Title"] = "Ürün Raporu";
}

<div x-data="productReport()" @@customer-selected.window="handleCustomerSelection($event.detail)" @@product-selected.window="handleProductSelection($event.detail)" class="space-y-6">
    <!-- Header -->
    <div class="md:flex md:items-center md:justify-between">
        <div class="flex-1 min-w-0">
             <h2 class="text-2xl font-bold leading-7 text-gray-900 sm:text-3xl sm:truncate">
                Ürün Sorgusu
            </h2>
        </div>
    </div>

    <!-- Main Query Card -->
    <div class="bg-white shadow sm:rounded-lg">
        <div class="px-4 py-5 sm:p-6">
            <div class="grid grid-cols-1 gap-y-6 gap-x-4 sm:grid-cols-6">
                
                <!-- Date Range -->
                <div class="sm:col-span-3">
                    <label for="start-date" class="block text-sm font-medium text-gray-700">Başlangıç Tarihi</label>
                    <div class="mt-1">
                        <input type="date" name="start-date" id="start-date" class="shadow-sm focus:ring-indigo-500 focus:border-indigo-500 block w-full sm:text-sm border-gray-300 rounded-md" value="@Model.StartDate.ToString("yyyy-MM-dd")">
                    </div>
                </div>

                <div class="sm:col-span-3">
                    <label for="end-date" class="block text-sm font-medium text-gray-700">Bitiş Tarihi</label>
                    <div class="mt-1">
                        <input type="date" name="end-date" id="end-date" class="shadow-sm focus:ring-indigo-500 focus:border-indigo-500 block w-full sm:text-sm border-gray-300 rounded-md" value="@Model.EndDate.ToString("yyyy-MM-dd")">
                    </div>
                </div>

                <!-- Product Group -->
                <div class="sm:col-span-6">
                    <label for="product-group" class="block text-sm font-medium text-gray-700">Ürün Grubu</label>
                    <div class="mt-1">
                        <select id="product-group" name="product-group" x-model="productFilter.productGroup" class="shadow-sm focus:ring-indigo-500 focus:border-indigo-500 block w-full sm:text-sm border-gray-300 rounded-md">
                            <option value="">Seçiniz</option>
                            <option value="Group1">Video Kurgu</option>
                            <option value="Group2">Ses & Miksaj</option>
                            <option value="Group3">Color Grading</option>
                        </select>
                    </div>
                </div>

                <!-- Product Selection -->
                <div class="sm:col-span-6">
                     <label class="block text-sm font-medium text-gray-700">Ürün</label>
                     <div class="mt-1 flex rounded-md shadow-sm">
                        <div class="relative flex-grow focus-within:z-10">
                             <input type="text" readonly x-model="selectedProductName" class="focus:ring-indigo-500 focus:border-indigo-500 block w-full rounded-none rounded-l-md sm:text-sm border-gray-300" placeholder="Tüm Ürünler">
                        </div>
                        <button type="button" @@click="$dispatch('product-select-open', { targetId: 'mainProduct' })" class="-ml-px relative inline-flex items-center space-x-2 px-4 py-2 border border-gray-300 text-sm font-medium rounded-r-md text-gray-700 bg-gray-50 hover:bg-gray-100 focus:outline-none focus:ring-1 focus:ring-indigo-500 focus:border-indigo-500">
                            <svg class="h-5 w-5 text-gray-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                                <path fill-rule="evenodd" d="M8 4a4 4 0 100 8 4 4 0 000-8zM2 8a6 6 0 1110.89 3.476l4.817 4.817a1 1 0 01-1.414 1.414l-4.816-4.816A6 6 0 012 8z" clip-rule="evenodd" />
                            </svg>
                             <span>Seç</span>
                        </button>
                    </div>
                </div>

                <!-- Customer Selection -->
                <div class="sm:col-span-6">
                     <label class="block text-sm font-medium text-gray-700">Müşteri</label>
                      <div class="mt-1 flex rounded-md shadow-sm">
                        <div class="relative flex-grow focus-within:z-10">
                             <input type="text" readonly x-model="selectedCustomerName" class="focus:ring-indigo-500 focus:border-indigo-500 block w-full rounded-none rounded-l-md sm:text-sm border-gray-300" placeholder="Tüm Müşteriler">
                        </div>
                        <button type="button" @@click="$dispatch('customer-select-open', { targetId: 'main' })" class="-ml-px relative inline-flex items-center space-x-2 px-4 py-2 border border-gray-300 text-sm font-medium rounded-r-md text-gray-700 bg-gray-50 hover:bg-gray-100 focus:outline-none focus:ring-1 focus:ring-indigo-500 focus:border-indigo-500">
                             <svg class="h-5 w-5 text-gray-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                                <path fill-rule="evenodd" d="M8 4a4 4 0 100 8 4 4 0 000-8zM2 8a6 6 0 1110.89 3.476l4.817 4.817a1 1 0 01-1.414 1.414l-4.816-4.816A6 6 0 012 8z" clip-rule="evenodd" />
                            </svg>
                             <span>Seç</span>
                        </button>
                    </div>
                </div>

            </div>
            
            <!-- Action Buttons -->
            <div class="mt-8 flex justify-end space-x-3">
                 <button type="button" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                    Detaylı Rapor Bas
                </button>
                <button type="button" class="inline-flex items-center px-4 py-2 border border-gray-300 shadow-sm text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                    Özet Rapor Bas
                </button>
            </div>
        </div>
    </div>

    <!-- Product And Customer Modals -->
    <partial name="_ProductSelectModal" />
    <partial name="_CustomerSelectModal" />
</div>

<script>
    function productReport() {
        return {
            selectedProductName: '',
            selectedProductCode: '',
            
            selectedCustomerName: '',
            selectedCustomerCode: '',

            productFilter: {
                // Just keeping any local filter state if needed for the main page, though mostly handled in modal
                productGroup: ''
            },
            
            init() {
                // Init
            },
            
            // Shared Modal Handlers
            handleCustomerSelection(detail) {
                 if(detail.targetId === 'main') {
                    this.selectedCustomerName = detail.customer.name;
                    this.selectedCustomerCode = detail.customer.code;
                 }
            },
            
            handleProductSelection(detail) {
                if(detail.targetId === 'mainProduct') {
                    this.selectedProductName = detail.product.name;
                    this.selectedProductCode = detail.product.code;
                }
            }
        }
    }
</script>
